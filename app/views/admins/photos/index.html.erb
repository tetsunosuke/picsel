<center>
<h3>Admin Photos Index</h3>
</center>
<% @photos.each do |photo| %>
        <%= link_to admins_photo_path(photo) do  %>
            <%= attachment_image_tag photo,:image,fallback: "noimage.jpg", style:'width: 15%;height:15%;padding:10px;' %>
        <% end %>
    <% end %>
</div>
