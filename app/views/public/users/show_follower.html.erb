 <% @user.followers.each do |user| %>
    <table>
        <tr>
            <td>  <%= user.username %> </td>
            <td><%= link_to '詳細',　public_photos_path(user) %></td>
        </tr>
    </table>
<% end %>